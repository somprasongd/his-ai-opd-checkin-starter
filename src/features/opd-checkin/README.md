# OPD Check-in feature workspace

This folder is intentionally sparse.

During the workshop, reusable product components for US-001 should normally live here, paired with Storybook stories when the component has meaningful reviewable states.

Example shape after the workshop (illustrative, not mandatory):

```text
src/features/opd-checkin/
├── PatientSearch.tsx
├── PatientSearch.stories.tsx
├── PatientCard.tsx
├── PatientCard.stories.tsx
├── CheckInForm.tsx
└── CheckInForm.stories.tsx
```

The agent must inspect the repository and propose a plan before creating this structure.
